# Model Repository

This directory is mounted into NVIDIA Triton as `/models`.

No model weights are bundled. Operators must mount or add model artifacts only
after reviewing the model license, model card, and dataset limitations.
